import GovernmentLayout from "@/components/GovernmentLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <GovernmentLayout>{children}</GovernmentLayout>;
}

